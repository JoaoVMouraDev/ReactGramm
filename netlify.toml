[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"

# Redirecionamento para garantir que o React Router funcione corretamente
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"